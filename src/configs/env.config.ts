export const EnvConfig = {
    API_URL: "http://localhost:4000",
    // * don't put android client id here, you need to put web client id to get the id token and not get developer error
    GOOGLE_WEB_CLIENT_ID: "XXXXXXXXXXXXXXXXXXXX",
}
